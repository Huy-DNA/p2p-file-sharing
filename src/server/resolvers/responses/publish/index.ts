import { PublishResponse } from '../../../../common/protocol/response.js';
import net from 'net';

export function resolvePublishResponse(connection: net.Socket, publishResponse: PublishResponse) {
  
}