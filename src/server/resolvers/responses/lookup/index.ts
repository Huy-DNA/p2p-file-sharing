import { LookupResponse } from '../../../../common/protocol/response.js';
import net from 'net';

export function resolveLookupResponse(connection: net.Socket, lookupResponse: LookupResponse) {
  
}